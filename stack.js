// stack for array
const stackArray = [];

stackArray.push("hi");
stackArray.push("h2");
stackArray.pop();

// push and pop

//queue for array
const queueArray = [];
queueArray.unshift("q1");
queueArray.unshift("q2");
queueArray.pop();

// unshift and pop

console.log(queueArray);
